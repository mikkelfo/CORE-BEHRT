defaults:
  - features: defaults
  - loader: defaults
  - outcomes: defaults
  - paths: defaults

  - override hydra/hydra_logging: disabled  
  - override hydra/job_logging: disabled  
  - _self_
  
hydra:  
  output_subdir: null
  run:  
    dir: .

paths:
  encoded_suffix: outcomes
