defaults:
  - loader: defaults
  - features: defaults
  - handler: defaults
  - tokenizer: defaults
  - paths: defaults
  - excluder: defaults

  - override hydra/hydra_logging: disabled  
  - override hydra/job_logging: disabled  
  - _self_
  
hydra:  
  output_subdir: null
  run:  
    dir: .

paths:
  encoded_prefix: pretrain

split:
  mode: random
