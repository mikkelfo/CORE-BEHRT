defaults:
  - loader: defaults
  - features: defaults
  - handler: defaults
  - outcomes: defaults  # Needed for outcomes.py (not used in data_pretrain)
  - tokenizer: defaults
  - paths: defaults

  - override hydra/hydra_logging: disabled  
  - override hydra/job_logging: disabled  
  - _self_
  
hydra:  
  output_subdir: null
  run:  
    dir: .

paths:
  encoded_suffix: pretrain

split:
  mode: random
